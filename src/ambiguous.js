//import { nahuatlLexicon } from './lexicon.js';

// --- Known Ambiguous Words ---
export const knownAmbiguousWords = [
    {
        //word: "tamol",
        //parse: [
        //    { morpheme: "to", details: nahuatlLexicon.find(m => m.morpheme === "to" && m.type === "prefix") },
        //    { morpheme: "amol", details: nahuatlLexicon.find(m => m.morpheme === "amol" && m.type === "noun_stem") }
        //]
    },
];
